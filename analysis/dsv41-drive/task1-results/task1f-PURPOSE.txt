task1f: ONE traced arm at gen4. Written 2026-09-21 before the arm runs (nothing of task1f has run).

WHAT IT IS. One arm, new:on:T (mirrors on, TRACED), graph decode, the workload and config of task1-2-new-on-T (same script cmd: 4 sessions, 256 prompt / 128 new tokens, GRAPH_GATHER=1, 70 GiB tier, the arm script's own env), at python tree 06f7428fdb5b57a2c28bca3da5f2c4cfadd56676 (gen4), EXPECT_NEW=9d5bfc184b482d4fcaf32a0fbee796063875d686 (its python tree is the same as HEAD's). Label task1f, so the arm is task1f-0-new-on-T. No series, no repeat.
WHAT IT IS FOR. Placement, not timing: schema 4's request.lanes (4e63616666) replaces the estimated k = min(6, max(m, round(vram_miss/40))) behind the Task 6 headline figures; analysis/dsv41-drive/per_row_recompute/schema4_lanes.py recomputes them from measured lane counts, and refuses a trace that is not schema 4.
WHAT IT IS NOT. It is not a baseline arm, not a timing result and not a cell of any comparison. A traced arm runs slower than an untraced one and the box is contended; decode tok/s, TTFT and the verdict's VALID / CONTENDED / OUTLIER / CROSS-ARM / GENERATION-mismatch lines are recorded but MUST NOT be quoted as a speed or as a two-bank / gen comparison, and the baseline cells' inconclusive rules do not apply to it. Contention is accepted (a different standard from the baseline arms, on purpose).
WHEN THE TRACE IS USABLE. schema 4 on the ram_miss_request lines; the number of graph_step lines and dropped_before are reported (the full run is 495 steps, as in task1-2; a partial run gives a usable prefix). If the arm aborts, INVALID for the drive-idle probe or otherwise, the trace is judged on those properties, not on the verdict.
FIRST OBSERVATION EXPECTED FROM IT. It is the first arm since the harness gate and the harness provenance record: its cache.json should carry runtime_sha256 {arm_script, verdict, env_full_sh, gpu_run_sh} and its json provenance.harness_loaded (files with sha256, harness worktree head/dirty); its verdict should print a NOTE HARNESS line. Until seen there, those are not verified.
